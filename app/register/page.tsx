// Live registration flow (multi-step form + payment) from the backend core.
// Requires MONGODB_URI to function; pricing comes from conference.config.ts.
// A premium "opens soon" placeholder is available in git history if needed.
import RegisterPage from '@/conference-backend-core/app/register/page'

export default RegisterPage
